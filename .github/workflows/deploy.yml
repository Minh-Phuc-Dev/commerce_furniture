name: Deploy

on:
  push:
    branches: [main]

jobs:
  setup-workspace:
    runs-on: self-hosted
    steps:
      - name: Setup workspace
        run: |
          mkdir -p /opt/furniture
  react-deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Copy source to /opt
        run: |
          rm -rf /opt/furniture/React
          mkdir -p /opt/furniture/React
          cp -r ./React /opt/furniture/React
  node-deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Copy source to /opt
        run: |
          rm -rf /opt/furniture/Node
          mkdir -p /opt/furniture/Node
          cp -r ./Node /opt/furniture/Node
